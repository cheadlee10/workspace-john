# Personal CRM Dependencies

# Core
sqlite3  # Built-in to Python

# Gmail API (optional - for email scanning)
google-auth-oauthlib>=1.0.0
google-api-python-client>=2.100.0

# Calendar API (optional - for meeting sync)
# Uses same google-api-python-client as Gmail

# Semantic search (optional - for vector embeddings)
# sentence-transformers>=2.2.0

# Testing
pytest>=7.4.0
